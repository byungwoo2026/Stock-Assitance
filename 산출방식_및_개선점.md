# 나만의 AI 투자 조수 대시보드 — 항목별 산출방식 및 개선점

> 작성 기준: `app.py` (2026-07-21 시점 코드) 전체 리뷰
> 목적: 각 화면에 표시되는 수치/리스트가 **실제로 어떤 데이터와 로직으로 산출되는지** 명확히 하고, 정확도를 낮추는 지점을 식별하여 개선 우선순위를 정한다.

---

## 0. 데이터 소스 요약

| 소스 | 용도 | 특징 |
|---|---|---|
| `polling.finance.naver.com` (JSON API) | KOSPI/KOSDAQ 실시간 지수 | 부호(상승/하락)가 코드로 명시되어 있어 안정적 |
| `finance.naver.com` HTML 스크래핑 | 시가총액 상위, 상한가, 순매수 상위, 애널리스트 리포트, 개별종목 펀더멘털 | 페이지 구조 변경 시 깨질 수 있음 |
| `FinanceDataReader` (`fdr`) | 주가/ETF 시세 이력, RSI/MACD 계산용 원천 데이터 | 내부적으로 네이버 데이터 사용 |
| Google News RSS | 종목/시장 관련 뉴스 헤드라인 | 검색 키워드 기반, 언론사 신뢰도 구분 없음 |
| Groq API (`llama-3.3-70b-versatile`) | AI 시장 브리핑 / 개별종목 심층분석 | 위 데이터를 근거로 한 **생성 텍스트** (사실 검증 기능 없음) |
| 하드코딩된 리스트 | 가치재평가주, 애널리스트 필터 대상 3개사, 퀀트 리스트 샘플 10종목 | 코드에 고정, 시장 상황과 무관하게 유지됨 |

---

## 1. 종합 대시보드

### 1-1. KOSPI / KOSDAQ 지수
- **함수**: `fetch_market_index()`
- **소스**: 네이버 실시간 지수 JSON API (`/api/realtime/domestic/index/{KOSPI|KOSDAQ}`)
- **로직**: `closePrice`(현재가), `fluctuationsRatio`(등락률), `compareToPreviousPrice.code`(2=상승, 5=하락)를 조합해 부호를 명시적으로 결정
- **캐시**: 5분 (`ttl=300`)
- **비고**: 과거 HTML 텍스트("상승"/"하락" 문자열) 판별 방식에서 방향이 반대로 표시되는 버그가 있어 JSON API 방식으로 교체됨 (2026-07-20)

### 1-2. 최근 1달 자금 유입 TOP 5 업종
- **함수**: `fetch_1month_sector_trends()`
- **로직**: 10개 업종을 대표하는 ETF 1종목씩 지정 → 최근 22영업일(약 1개월) 시작~종료 종가 수익률 계산 → 수익률 내림차순 정렬 후 상위 5개
- **캐시**: 1시간
- **⚠️ 한계**: "자금 유입"이라는 이름과 달리 실제 순매수 자금(수급 데이터)이 아니라 **ETF 가격 수익률로 간접 추정**한 값. 업종을 대표 ETF 1종목에 의존하므로, 해당 ETF의 개별 이슈(추종 오차, 유동성 부족 등)가 업종 전체를 왜곡할 수 있음.

### 1-3. AI 투자 비서 브리핑
- **함수**: `get_market_ai_briefing()`
- **로직**: 위 1-1, 1-2 데이터를 프롬프트에 넣어 Groq에게 해설/전략 텍스트 생성 요청
- **비고**: 원본 지수/업종 데이터가 정확하면 해석도 논리적으로 일관되지만, **AI가 실시간 뉴스나 외부 팩트를 조회하지 않고 주어진 숫자만으로 추론**하므로 "투자 전략 제안" 등 조언성 문장은 매 호출마다 표현이 달라질 수 있고 사실 검증되지 않음.

---

## 2. 시장 자금 & 업종 분석

### 2-1. 최근 1달 자금 유입 5대 업종
- 1-2와 동일 로직 재사용

### 2-2. 외국인/기관 연속 순매수 분석
- **함수**: `fetch_net_buying_top()`
- **소스**: `finance.naver.com/sise/sise_deal_rank.naver` (투자자구분 파라미터로 외국인/기관 전환)
- **로직**: 해당 페이지에 노출된 순매수 상위 종목명을 순서대로 추출 (상위 10개)
- **매수 사유 표시**: 각 종목명으로 Google News RSS 검색 → **최상단 뉴스 제목 1건**을 "매수 사유"로 표시
- **⚠️ 한계**: 표시되는 "매수 사유"는 실제 매수와 인과관계가 검증된 것이 아니라, 단순히 그 종목명으로 검색했을 때 가장 최근에 걸리는 뉴스 제목일 뿐. 뉴스가 없으면 "기술적/패시브 자금 매수 추정"이라는 정성적 문구로 대체됨.

---

## 3. 주요 기업 헤드라인 뉴스

### 3-1. 시가총액 상위 기업 뉴스
- **함수**: `fetch_top_market_cap()` — `sise_market_sum.naver` 페이지에서 시총 순으로 나열된 종목명 상위 N개 추출 (캐시 1시간)
- 종목 선택 시 `fetch_headlines_rss()`로 Google News 검색, 상위 5건 + (선택 시) AI 3줄 요약(`get_ai_summary()`)

### 3-2. 상한가 종목 및 이슈
- **함수**: `fetch_upper_limit_stocks()` — `sise_upper.naver`(전 거래일 상한가 페이지) 스크래핑
- **비고**: "전 거래일 기준"이라는 안내 문구가 있으나, 실제로는 접속 시점에 네이버가 보여주는 상한가 페이지를 그대로 가져오는 것이라 장중에는 당일 데이터로 바뀔 수 있음(시점 표기 불명확)

---

## 4. 외인 수급 & 기술적 조건 스크리너

- **함수**: `run_logical_screener()`
- **1단계 — 대상 추출**: `sise_quant.naver`(거래량 상위) 페이지에서 상위 100종목 추출 → "시장 주도주" 풀로 정의
- **2단계 — 지표 계산** (각 종목당 150일치 OHLCV → 최근 100개 사용)
  - `SMA20`, `SMA60`: 단순이동평균
  - `RSI(14)`: EWM(com=13) 방식 (Wilder's Smoothing 근사)
  - `MACD(12,26,9)`: EMA12 − EMA26, Signal = MACD의 EMA9
- **3단계 — 점수화 (100점 만점)**
  - 이동평균 배열 상태: 정배열(가격>SMA20>SMA60) 40점 / SMA20>SMA60만 25점 / 가격>SMA20만 15점 / 그 외 0점
  - MACD 상태: MACD>Signal 30점 / MACD>0(조정중) 10점 / 그 외 0점
  - RSI 상태: ≤40 30점 / 40~55 20점 / 55~70 10점 / 70초과 **-10점(감점)**
  - 점수 내림차순 상위 20개 표시
- **캐시**: 1시간
- **⚠️ 한계**: "거래량 상위 100종목"이라는 풀 자체가 이미 일정 편향(대형주·이슈주 쏠림)을 가지므로, 거래량이 적은 숨은 우량주는 애초에 후보에서 배제됨. 또한 100영업일 데이터 중 앞부분은 이동평균/RSI/MACD가 워밍업(warm-up) 구간이라 계산 정확도가 낮으나, 최신값만 사용하므로 이 자체는 큰 문제는 아님.

---

## 5. 최우수 애널리스트 추천 종목

- **함수**: `fetch_top_analyst_recommendations()`
- **소스**: `finance.naver.com/research/company_list.naver`
- **로직**: 네이버 리포트 목록에는 애널리스트 실명이 없어 개인 단위 필터링이 불가하므로, **매일경제 베스트 애널리스트 평가(리서치센터 부문) 상위 3개사(신한투자증권/하나증권/메리츠증권)로 하드코딩 필터링**한 뒤 발간된 리포트만 노출
- **캐시**: 1시간
- **⚠️ 한계**: 3개사 명단이 코드에 고정되어 있어, 매일경제 평가 순위가 갱신되어도 **코드를 수동으로 바꾸기 전까지는 자동 반영되지 않음**. "최우수 애널리스트"라는 이름과 달리 실제로는 "특정 3개 증권사 리서치센터"가 기준.

---

## 6. 가치재평가주 (⚠️ 실시간 데이터 아님)

- **저PBR 10선 / 매출이익률 40%↑ 10선 / 매출성장률 50%↑ 10선** 세 리스트 모두 **코드에 종목명이 하드코딩**되어 있음 (예: 저PBR → KB금융, 하나금융지주… 매출성장률 → 에코프로비엠, 포스코퓨처엠… 등)
- **아무런 실시간 계산이나 필터링을 거치지 않고, 화면에 그대로 출력됨**
- 화면 내 안내 문구도 "전 종목 실시간 전수조사는 연산 부담이 커 대표 종목을 큐레이션 제공"이라고 명시하고 있어, 개발자 스스로도 이 부분이 정적 리스트임을 인지하고 있는 상태
- **정확도 영향**: 시장 상황이 바뀌어도(예: 특정 종목이 더 이상 저PBR이 아니게 되어도) 리스트가 갱신되지 않음. 사실상 "예시 리스트"에 가까움.

---

## 7. 퀀트 투자 리스트

- **함수**: `build_quant_filter_candidates()`
- **⚠️ 핵심 한계**: 전 종목이 아니라 **삼성전자, SK하이닉스, NAVER, 카카오, 셀트리온, LG화학, 삼성SDI, 삼성바이오로직스, 비에이치, 카카오페이 — 딱 10개 대형주만** 후보 풀로 사용
- 각 종목의 PER/PBR/ROE/영업이익률은 `fetch_stock_name_and_fundamentals()`로 실제 조회하지만, **`시가총액`과 `매출성장률` 필드는 코드에서 항상 `None`으로 채워짐** (실제 조회 로직이 없음)
- 필터 슬라이더 로직이 `.isna() | (조건)` 형태라서, 값이 `None`이면 **조건과 무관하게 항상 통과** → 사용자가 "시가총액 최소값", "매출성장률 하한선"을 조정해도 **실질적으로 아무 필터링 효과가 없음**
- `업종` 필드도 전부 "기타"로 고정되어 있어, "업종 키워드" 필터도 "기타"를 입력하지 않는 한 전부 걸러짐
- **정리**: 현재 이 메뉴는 "10개 대형주 중 PER/PBR/ROE/영업이익률 조건만 실질적으로 작동하는 미니 스크리너"에 가까움. 이름(퀀트 투자 리스트)이 주는 기대(전 종목 대상 정량 스크리닝)와 실제 동작 사이 괴리가 가장 큰 메뉴.

---

## 8. 개별종목분석

### 8-1. 펀더멘털 데이터
- **함수**: `fetch_stock_name_and_fundamentals()`
- **소스**: `finance.naver.com/item/main.naver?code=...`
- **추출 항목**: PER, PBR, 추정(컨센서스) PER, 동일업종 PER, 영업이익률, ROE (재무제표 표에서 파싱)
- **캐시**: 60초 (개별 종목 조회이므로 짧게 설정)

### 8-2. 기술적 지표
- **함수**: `analyze_stock_technical()`
- **소스**: `fdr.DataReader`로 150일치 OHLCV → 최근 100개 사용
- **산출**:
  - `SMA20`, `SMA60`
  - `RSI(14)` (EWM com=13)
  - `MACD(12,26,9)`
  - **1개월 수익률**: 최근 종가 대비 22영업일 전 종가 변화율
  - **거래량 비율**: 당일 거래량 ÷ 최근 5일 평균 거래량 × 100

### 8-3. 종합 점수 (`calculate_stock_score()`, 100점 만점)
| 항목 | 배점 | 세부 로직 |
|---|---|---|
| 뉴스/수급 | 40점 | 뉴스 제목의 긍정/부정 키워드 매칭(언론사 신뢰도 가중 2배) → 감성점수 20점 + 노출빈도 10점 + 거래량모멘텀 10점 |
| 밸류에이션 | 10점 | 동일업종 대비 상대 PER(최대 6점) + PBR 구간별 가점(최대 4점) |
| 시장 트렌드 | 20점 | 1개월 수익률 구간별 점수 (5%↑=20점, 0~5%=15점, -10%↓=0점, 그 외 10점) |
| 기술적 지표 | 10점 | 가격>SMA20 & MACD>Signal=10점, RSI≤40=8점, 그 외 5점 |
| 경영지표 | 20점 | 영업이익률(최대 12점) + ROE(최대 8점) 구간별 점수 |

- **⚠️ 뉴스 감성 판정 한계**: 키워드 매칭 방식(`공급계약`, `흑자전환` 등 포함 여부)이라 문맥을 이해하지 못함. 예: "적자 축소" 기사는 "적자"라는 부정 키워드에 걸려 오분류될 수 있음.
- **⚠️ 코드 중복 위험**: "개별종목 분석" 탭(8-4)에서는 화면 표시용으로 `trend_score`/`tech_score`/`mgmt_score`를 `calculate_stock_score()`와 **별도로 동일 로직을 다시 작성**해 계산하고 있음. 향후 점수 체계를 수정할 때 두 곳을 동시에 고치지 않으면 총점과 세부 항목 표시가 서로 어긋날 수 있음.

### 8-4. AI 심층 분석 리포트
- **함수**: `get_individual_stock_ai_analysis()` — 위 8-1~8-3 데이터를 근거로 Groq가 재무/차트/뉴스/전략 4단 리포트 생성

---

## 9. ETF List (개별종목분석 내 탭)

| 리스트 | 함수 | 로직 |
|---|---|---|
| 자금유입(거래대금) 상위 10선 | `fetch_etf_market_data()` | `fdr.StockListing('ETF/KR')` 전체 ETF의 `Amount`(거래대금) 내림차순 상위 10개 — 실제 거래대금 순위 |
| 1주일 수익률 상위 10선 | `fetch_etf_weekly_returns()` | 레버리지/인버스/2X 상품 제외 → 거래대금 상위 50개 중 최근 10일 시세로 수익률 계산 → 상위 10개 |
| 신규 상장 추정 ETF 10선 | (인라인 로직) | 네이버 ETF 목록 API에 상장일 필드가 없어, **종목코드 내림차순 정렬로 근사** (라벨에 "추정"임을 명시) |

---

## 10. 캐시(TTL) 현황 요약

| 함수 | TTL |
|---|---|
| `fetch_market_index` | 5분 |
| `fetch_stock_name_and_fundamentals` | 60초 |
| `fetch_1month_sector_trends`, `fetch_top_market_cap`, `fetch_upper_limit_stocks`, `fetch_net_buying_top`, `fetch_top_analyst_recommendations`, `run_logical_screener`, `fetch_etf_weekly_returns`, `build_quant_filter_candidates` | 30분~1시간 |
| `fetch_etf_market_data` | 30분 |
| `get_stock_code_map` (KRX 전종목 코드) | 24시간 |

→ 화면마다 실시간성이 다르므로, "방금 본 숫자가 몇 분 전 데이터인지" 사용자가 인지하기 어려움. 표 상단에 "최근 갱신: N분 전" 같은 표시를 추가하면 개선 가능.

---

## 11. 정확도 개선 우선순위 제안

| 우선순위 | 항목 | 문제 | 개선 방향 |
|---|---|---|---|
| 1 (높음) | 퀀트 투자 리스트 | 시가총액/매출성장률 필터가 항상 무력화, 업종도 "기타" 고정, 대상이 10종목뿐 | `fdr.StockListing('KRX')`로 전종목 시가총액 확보, 매출성장률은 재무제표 3개년 비교 로직 추가 필요 |
| 2 (높음) | 가치재평가주 | 완전 정적 하드코딩, 실제 스크리닝 아님 | 저PBR은 `fdr` 전종목 PBR 정렬로 즉시 실데이터화 가능, 매출성장률/이익률 조건은 재무데이터 배치수집 필요 |
| 3 (중간) | 애널리스트 추천 | 3개 증권사 고정, 매경 순위 변경 시 수동 갱신 필요 | 최소한 화면에 "기준 연도/순위 갱신일" 명시 |
| 4 (중간) | 뉴스 감성 점수 | 키워드 매칭이라 문맥 오분류 가능 | Groq에게 감성 판정 자체를 맡기고 결과를 점수화(현재는 키워드 매칭 후 AI는 요약만 별도 생성) |
| 5 (낮음) | 신규상장 ETF | 코드순 근사치 | 공공데이터포털 API 연동 시 정확한 상장일 확보 가능(회원가입 필요) |
| 6 (낮음) | 캐시 표시 | 갱신 시점 불명확 | 각 표/카드에 최근 갱신 시각 캡션 추가 |

---

*이 문서는 코드 정적 분석을 기준으로 작성되었으며, 실제 운영 중 네이버/Groq 응답 포맷이 변경될 경우 위 로직 설명과 실제 동작이 달라질 수 있습니다.*
