# Stock_Assitance (나만의 AI 투자 조수 대시보드) — 프로젝트 정리

> 최종 정리 기준: 2026-07-27
> 이 문서는 (1) 지금까지 진행한 작업 히스토리와 (2) 현재 프로젝트의 세부 구조를 정리한 것입니다.

---

## 1. 프로젝트 기본 정보

| 항목 | 내용 |
|---|---|
| 프로젝트명 | 나만의 AI 투자 조수 대시보드 (Stock_Assitance) |
| 성격 | [[stock-agent]] 프로젝트와는 별개의 독립 프로젝트 |
| 로컬 경로 | `C:\민병우\공부\3. 코디세이\Stock_Assitance` |
| GitHub 저장소 | `byungwoo2026/Stock-Assitance` |
| 배포 | Streamlit Cloud (`stock-assitance-...streamlit.app`) |
| 개발 환경 | Windows / VS Code / PowerShell / `.venv` 가상환경 |
| 메인 파일 | `app.py` (약 1,600줄) |
| AI 엔진 | Groq API (`llama-3.3-70b-versatile`) — 기존에 발급받아둔 `GROQ_API_KEY` 재사용 |
| requirements.txt | streamlit==1.58.0, pandas, beautifulsoup4, requests, groq, finance-datareader |
| Git 운영 방식 | 로컬은 `master` 브랜치 사용, Streamlit Cloud는 `main` 브랜치를 보고 배포 → 매 변경마다 `git push` + `git push origin master:main` 두 번 필요 |

---

## 2. 작업 히스토리 (시간순)

### 2-1. AI 엔진 교체 (Gemini → Groq)
- **문제**: 배포된 앱에서 `404 models/gemini-1.5-flash is not found` 에러 발생 (구글이 해당 모델을 지원 종료)
- **조치**: `google.generativeai` 코드를 전부 `groq` 라이브러리 기반으로 교체 (`get_ai_summary`, `get_individual_stock_ai_analysis`, `get_market_ai_briefing` 3개 함수)
- **부수 이슈**: `requirements.txt`에서 `google-generativeai` 제거, `groq` 추가 필요 — 처음엔 로컬 파일 덮어쓰기가 누락되어 재작업
- **Git 이슈**: 로컬 `master`에는 반영됐지만 Streamlit Cloud가 보는 `main` 브랜치에 반영이 안 돼 배포가 안 되던 문제 → `git push origin master:main`으로 해결, 이후 매 배포마다 반복되는 절차로 정착

### 2-2. KOSPI/KOSDAQ 지수 상승/하락 표시 오류 수정
- **문제**: 실제로는 하락했는데 화면에는 "상승"으로 표시됨
- **원인**: 네이버 HTML의 "상승"/"하락" 텍스트를 문자열로 판별하는 로직이 실제 방향과 반대로 매칭
- **조치**: 네이버 실시간 지수 JSON API(`polling.finance.naver.com/api/realtime/domestic/index/{KOSPI|KOSDAQ}`)로 교체 — `compareToPreviousPrice.code`(2=상승, 5=하락) 필드로 명확하게 방향 판정
- **추가**: 한국 증시 관례에 맞춰 상승=빨강/▲, 하락=파랑/▼로 커스텀 HTML 표시 적용 (Streamlit 기본 `st.metric`은 초록/빨강만 지원해 미지원)

### 2-3. 전체 메뉴 산출방식 문서화 및 정확도 점검
- 앱 전체 코드를 리뷰하여 메뉴별 데이터 산출 방식, 캐시 주기, 정확도 취약점을 정리한 문서 작성
- 이 리뷰에서 다음 두 가지 심각한 문제를 발견:
  1. **퀀트 투자 리스트**: 시가총액/매출성장률/업종 필터가 실제로는 항상 빈 값이라 작동하지 않음, 대상도 10개 대형주로 고정
  2. **가치재평가주**: 3개 탭 전부 종목명이 코드에 하드코딩되어 실시간 계산이 아님

### 2-4. 퀀트 투자 리스트 / 가치재평가주 실데이터화
- `fetch_market_universe()`: `fdr.StockListing('KRX')`(실제 시가총액) + `fdr.StockListing('KRX-DESC')`(실제 업종) 전종목 데이터 확보 (6시간 캐시)
- `fetch_stock_name_and_fundamentals()` 확장: 네이버 재무제표 표에서 연간 매출액/영업이익률 시계열을 추가로 파싱해 **실제 매출성장률 평균**, **영업이익률 평균**을 계산
- `scan_fundamentals()`: `ThreadPoolExecutor`로 여러 종목의 펀더멘털을 병렬 조회
- **퀀트 투자 리스트**: 시가총액/업종 조건으로 먼저 후보를 좁힌 뒤, "스캔 대상 최대 종목 수"(기본 150개, 조절 가능)만큼만 실시간 스캔. 버튼(🔍) 클릭 시에만 실행되도록 변경
- **가치재평가주**: 하드코딩 리스트 완전 제거, "스캔 대상 시가총액 상위 N개"(기본 300개)를 실시간 스캔해 저PBR/고수익성/고성장 상위 10개씩 계산. 각 표에 "확인 연도 수"를 표시해 몇 개년 데이터로 평균낸 것인지 투명하게 공개
- **한계**: 스캔 범위를 "시가총액 상위 N개"로 한정하다 보니 중소형 고성장주가 범위 밖일 수 있음 (화면에 안내 문구로 명시)

### 2-5. 최우수 애널리스트 추천 종목 개선
- 대상 증권사를 3개사(신한투자증권/하나증권/메리츠증권) → **5개사**(+ KB증권, NH투자증권)로 확대
- **문제 1**: 결과가 특정 증권사(하나증권 등)에 쏠림 → 페이지 1개만 조회하던 것을 여러 페이지 순회 + 증권사별 최대 4건 제한 + 라운드로빈으로 균형 있게 구성하도록 수정
- **문제 2**: 그래도 KB증권/메리츠증권이 거의 안 보임 → 실제로 해당 증권사들이 네이버 리포트 목록에 리포트를 게시하는 빈도 자체가 낮다는 것을 확인 (사용자 직접 검증). 매경 베스트 5사 기준은 유지하되, 스캔 페이지 수를 5 → 30 → **60페이지**로 확대하는 것으로 최종 결정
- 화면에 "선정 기준"과 "리스트 최종 확인/갱신일"을 명시해 투명성 확보

### 2-6. 뉴스 감성 점수 로직 개선
- 기존: 단순 키워드 매칭 (예: "적자"라는 단어가 있으면 무조건 부정으로 판정 → "적자 축소"처럼 실제로는 긍정적인 기사도 오분류)
- 개선: `classify_news_sentiment_ai()` 함수 추가 — 종목 관련 뉴스 제목들을 Groq에게 한 번에 보내 문맥을 고려한 감성(긍정/중립/부정) 판정을 받음
- AI 판정 실패 시 기존 키워드 매칭 방식으로 자동 폴백하는 안전장치 포함

### 2-7. AI 시장 브리핑이 실제 뉴스를 근거로 작성하도록 개선
- **문제**: `get_market_ai_briefing()`이 KOSPI/KOSDAQ 지수와 업종 리스트, 즉 **숫자만 보고** AI가 그럴듯한 전략 문장을 지어내는 구조였음 (실제 뉴스/이벤트 미반영). 부수적으로 응답에 스페인어 단어("especialmente")가 섞이는 버그도 있었음
- **조치**: `fetch_headlines_rss("코스피 코스닥 증시")`로 오늘자 실제 시장 뉴스를 가져와 프롬프트에 포함, "뉴스에 없는 내용은 지어내지 말 것", "한국어로만 응답할 것" 등 명시적 지시 추가, `temperature=0.3`으로 낮춰 일관성 향상

### 2-8. 사이드바 드롭다운 → 상단 버튼형(가로 탭) 메뉴로 변경
- 기존 `st.sidebar.selectbox` 방식을 화면 상단의 가로 버튼형 메뉴로 교체
- 기존 `menu` 변수 기반 if/elif 로직은 그대로 유지한 채, `st.radio(horizontal=True)`를 CSS로 버튼/탭처럼 스타일링 (선택된 메뉴는 빨간 배경으로 강조)

### 2-9. 외국인/기관 연속 순매수 분석 — 근본 원인 파악 및 수정
- **증상**: 코스닥 쪽 데이터가 아예 안 뜨거나, 코스피 외국인/기관 데이터가 뒤죽박죽으로 보임
- **진단 과정**: 사용자가 브라우저 개발자도구(F12 → Network 탭)로 직접 실제 요청을 추적
- **발견 1**: `sosok` 파라미터가 정수 0/1이 아니라 **2자리 문자열 "01"(코스피)/"02"(코스닥)**여야 함 (정수로 보내면 404)
- **발견 2 (핵심)**: 데이터가 실제로 존재하는 곳은 겉 페이지(`sise_deal_rank.naver`)가 아니라 **iframe으로 별도 로드되는 `sise_deal_rank_iframe.naver`** 페이지였음. 기존 스크래핑은 겉 페이지만 요청해서 사이드바 위젯 링크 몇 개만 잘못 긁어오고 있었음
- **최종 URL 형식**: `sise_deal_rank_iframe.naver?sosok={01|02}&investor_gubun={9000|1000}&type=buy`
- **추가 처리**: 이 페이지도 "이전 영업일/최근 영업일" 두 날짜가 나란히 표시되는 구조라, `table.type_5`가 2개 이상 잡히면 가장 마지막(최근 날짜) 표만 사용하도록 처리
- **검증**: 외국인/기관 × 코스피/코스닥 4가지 조합 전부 실제 데이터가 뜨는 것을 스크린샷으로 확인 완료

### 2-10. ETF List 개선
- "1주일 수익률 상위 10선"에서 레버리지/인버스/2X 상품 제외 (일반 ETF만 비교하도록)
- "신규 상장 ETF 10선" → "신규 상장 **추정** ETF 10선"으로 라벨 수정 + "실제 상장일이 아닌 종목코드 기준 추정"이라는 안내 문구 추가 (실제 상장일 데이터를 무료로 안정적으로 구할 방법을 찾지 못해, 정직하게 라벨링하는 것으로 결론)

---

## 3. 현재 앱 구조 (메뉴별 요약)

| 메뉴 | 핵심 기능 | 데이터 소스 |
|---|---|---|
| 종합 대시보드 | KOSPI/KOSDAQ 지수, 업종 트렌드, AI 시장 브리핑 | 네이버 실시간 지수 API, 업종 대표 ETF 수익률, Groq + 실제 뉴스 |
| 시장 자금 & 업종 분석 | 1달 자금유입 업종, 외국인/기관 순매수 상위 | 업종 대표 ETF, `sise_deal_rank_iframe.naver` |
| 주요 기업 헤드라인 뉴스 | 시총 상위/상한가 종목 뉴스 | `sise_market_sum`/`sise_upper`, Google News RSS |
| 외인 수급 & 기술적 조건 스크리너 | 거래량 상위 종목 기술적 점수화 | `sise_quant`, `fdr` OHLCV, SMA/RSI/MACD |
| 최우수 애널리스트 추천 종목 | 5개 증권사 리포트 균형 노출 | `research/company_list.naver` (최대 60페이지 스캔) |
| 가치재평가주 | 저PBR/고수익성/고성장 실시간 스캔 | `fdr` 시가총액 전종목 + 개별 페이지 재무데이터 |
| 퀀트 투자 리스트 | 사용자 정의 조건 기반 전종목 스크리닝 | 위와 동일 |
| 개별종목분석 | 종목별 펀더멘털/기술적 지표/뉴스 감성/AI 리포트 종합 점수 | 네이버 개별 종목 페이지, `fdr`, Groq |

---

## 4. 알려진 한계 (현재 시점 기준)

- **가치재평가주/퀀트 리스트**: "시가총액 상위 N개"로 스캔 범위를 한정하므로, 중소형 고성장주가 범위 밖에 있을 수 있음
- **신규 상장 ETF**: 실제 상장일이 아닌 종목코드 기준 근사치 (정확한 데이터는 공공데이터포털 API 키 발급 시 가능)
- **애널리스트 추천**: 매경 베스트 5개사로 고정, 순위 갱신 시 코드 수동 갱신 필요. 일부 증권사(KB/메리츠)는 게시 빈도가 낮아 60페이지를 스캔해도 결과가 적거나 없을 수 있음
- **외국인/기관 순매수**: "기관" 조합의 iframe URL은 "외국인" 조합과 동일한 패턴일 것이라 가정하고 적용됨 (개발자도구로 직접 확인된 건 "외국인" 조합), 실사용 중 문제 재발 시 재확인 필요
- **업종 매칭**: 시가총액-업종 데이터 병합 시 일부 종목(우선주 등)이 "업종정보없음"으로 표시될 수 있음

---

## 5. 향후 개선 아이디어 (미착수)

- 뉴스 감성 판정을 Groq 기반으로 전환한 것처럼, 퀀트/가치재평가 스캔 범위를 시가총액 제한 없이 전종목으로 확대(성능과의 트레이드오프 고려 필요)
- 각 표/카드에 "최근 갱신 시각" 캡션 추가로 캐시 데이터의 실시간성 투명화
- 신규 상장 ETF 정확도 개선을 위한 공공데이터포털 API 연동 검토
